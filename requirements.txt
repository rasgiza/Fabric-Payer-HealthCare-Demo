# Fabric notebook runtime requirements (Spark / Python on Fabric).
#
# These pins target Fabric Runtime 1.3 (Spark 3.5 / Python 3.11). They are
# installed by the launcher into a Fabric Environment item at deploy time and
# referenced by every batch and RTI notebook so behaviour is identical across
# dev / staging / prod.
#
# Local-dev / CI pins live in requirements-dev.txt (pytest + ruff + tools);
# do NOT merge the two — Fabric's base image already ships pandas/numpy/duckdb
# at fixed versions and re-pinning them here would force a slow rebuild on
# every notebook session.

# Microsoft AI / Foundry stack (GA pieces). The new Foundry Agent Service (GA)
# uses the Responses API; the classic Assistants API surface and the
# `2026-04-01-preview` api_version are deprecated (retires 2027-03-31).
azure-ai-projects==1.0.0
azure-ai-agents==1.0.0
azure-identity==1.19.0
openai==1.55.0

# Microsoft Agent Framework 1.9.0 (latest; used by PayerOpsAgent + hosted
# Foundry agents). 1.10 does not exist on PyPI — do not bump until released.
agent-framework==1.9.0
agent-framework-azure-ai==1.9.0

# Fabric CI/CD (tools/deploy.py + GitHub Actions deploy job). v1.0.0 made
# token_credential mandatory; tools/deploy.py passes DefaultAzureCredential.
fabric-cicd==1.1.0

# Semantic Link / Power BI helpers (Direct Lake semantic-model checks + measures)
semantic-link==0.10.0
semantic-link-labs==0.10.0

# Healthcare / standards parsing
fhir.resources==7.1.0
hl7==0.4.5

# Real-time scoring helpers (used by NB_RTI_* notebooks; safe on driver only)
scikit-learn==1.5.2
